import { FC, memo } from "react";
import styles from "./Footer.module.css";

const Footer: FC<any> = () => {
  return (
    <>
        <div>
            Footer
        </div>
    </>
  );
};

export default memo(Footer);
